export enum MailboxType {
  GMAIL = "GMAIL",
  OUTLOOK = "OUTLOOK",
  SMTP = "SMTP",
  AMAZON_SES = "SES",
}

export enum MailboxConnectionStatus {
  PENDING = "PENDING",
  VERIFIED = "VERIFIED",
  UNVERIFIED = "UNVERIFIED",
}
